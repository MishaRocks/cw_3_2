[tool.poetry]
name = "app"
version = "0.1.0"
description = ""
authors = ["MishaRocks <tehobox@yandex.ru>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.9"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
